services:
  qwen2api-rs:
    build: .
    image: qwen2api-rs:latest
    container_name: qwen2api-rs
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    volumes:
      - ./data:/app/data
